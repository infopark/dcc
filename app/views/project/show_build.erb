<%= link_to 'Übersicht', :action => 'index' %>
<h1>Project <%= link_to project_display_value(@build.project), :action => 'show', :id => @build.project %>,
    Build <%= build_display_value(@build) %></h1>
<table class='buckets'>
<% buckets = @build.buckets
buckets.each do |bucket| -%>
  <tr>
    <td class='name'><%= link_to bucket_display_value(bucket), :action => "show_bucket", :id => bucket %></td>
    <td><%= bucket_display_status(bucket) %></td>
  </tr>
<% end -%>
</table>
